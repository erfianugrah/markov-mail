# Inventory

The legacy inventory file was removed in this reset. As we train and promote decision trees we’ll record their KV key + timestamp here (e.g., `decision-tree.2025-02-01.json`).

| Date | Artifact | Notes |
|------|----------|-------|
| _TBD_ | `config/production/decision-tree.latest.json` | First tree export placeholder |
